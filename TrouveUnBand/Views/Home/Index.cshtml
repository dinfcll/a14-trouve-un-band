@{
    ViewBag.Title = "Page d'accueil";
}
<div class="home-container">
    <div id="home-jumbotron" class="jumbotron">
        <div id="home-carousel" class="carousel slide" data-ride="carousel">
            <div class="carousel-inner">
                <div class="item active">

                    <img id="homeImage" src="" alt="">

                    <div class="carousel-caption">
                        <h1 class="col-md-12">À la recherche d'un band?</h1>
                        <h3>Recherchez un band, un musicien ou un événement.</h3>
                        <h3>Créez vos propres événements et annonces.</h3>
                        <h3>Faites la promotion de votre band, votre événement ou votre bar.</h3>
                        <h3>Tout ça GRATUITEMENT!</h3>
                        <p>@Html.ActionLink("Inscrivez-vous", "Register", "Users", routeValues: null, htmlAttributes: new { id = "registerLink", @class = "btn btn-primary btn-lg col-md-6 col-md-offset-3" })</p>
                    </div>

                </div>
            </div>
        </div>
    </div>

    <div class="container col-md-12">


    </div>
</div>

    @*<div id="NewsfeedUser" class="row">
        @foreach (var item in (List<TrouveUnBand.ViewModels.ResultViewModels>)ViewData["NewsfeedUser"])
        {
            <div class="col-sm-6 col-md-1 container-fluid NewsfeedItems">
                <div class="thumbnail card btn btn-default"  data-toggle="tooltip" data-placement="bottom" title="@Html.DisplayFor(modelItem => item.Name)">
                        <img src="@item.Photo" width="100" height="100" class="photo" alt="Image">
                </div>
                @Html.DisplayFor(modelItem => item.Name)
            </div>
        }
        </div>

        <div id="NewsfeedBand" class="row">
        @foreach (var item in (List<TrouveUnBand.ViewModels.ResultViewModels>)ViewData["NewsfeedBand"])
        {
            <div class="col-md-3 NewsfeedItems thumbnail">
                <img src="http://placehold.it/200x200">
                <div class="caption container-fluid">
                    <div class="row">
                    @Html.Label("Nom")
                    @Html.DisplayFor(modelItem => item.Name)
                    </div>
                    <div class="row">
                    @Html.Label("Location")
                    @Html.DisplayFor(modelItem => item.Location)
                    </div>
                    <div class="row">
                    @Html.Label("Description")
                    @Html.DisplayFor(modelItem => item.Description)
                    </div>
                    @Html.ActionLink("Afficher", "ViewProfile", "Search", new { type = "BAND", Id = item.ID }, new { @class = "btn btn-primary", @role = "button" })
                </div>
             </div>
        }
        </div>

        <div id="NewsfeedEvent" class="row">
            @foreach (var item in (List<TrouveUnBand.ViewModels.ResultViewModels>)ViewData["NewsfeedEvent"])
            {
                <div class="col-md-4 thumbnail">
                    <div class="thumbnail card">
                        <img src="@item.Photo" width="250" height="172" class="photo" alt="Image">
                    </div>
                    <div class="caption container-fluid">
                        <div class="row">
                            @Html.Label("Nom")
                            @Html.DisplayFor(modelItem => item.Name)
                        </div>
                        <div class="row">
                            @Html.Label("Location")
                            @Html.DisplayFor(modelItem => item.Location)
                        </div>
                        <div class="row">
                            @Html.Label("Genre")
                            <span>@item.Genres</span>
                        </div>
                        @Html.ActionLink("Afficher", "ViewProfile", "Search", new { type = "EVENT", Id = item.ID }, new { @class = "btn btn-primary", @role = "button" })
                    </div>
                </div>
            }
        </div>

        <div id="NewsfeedAdvert" class="row">
        @foreach (var item in (List<TrouveUnBand.ViewModels.ResultViewModels>)ViewData["NewsfeedAdvert"])
        {
            <div class="col-md-4 thumbnail">
                <div class="thumbnail card">
                    <img src="@item.Photo" width="250" height="172" class="photo" alt="Image">
                </div>
                <div class="caption container-fluid">
                    <div class="row">
                        @Html.Label("Type")
                        @Html.DisplayFor(modelItem => item.Type)
                    </div>
                    <div class="row">
                    @Html.Label("Genre")
                    @Html.DisplayFor(modelItem => item.Genres)
                    </div>
                    <div class="row">
                        @Html.Label("Description")
                        @Html.DisplayFor(modelItem => item.Description)
                    </div>
                    <div>
                        @Html.ActionLink("Afficher", "ViewProfile", "Search", new { type = "ADVERT", Id = item.ID }, new { @class = "btn btn-primary", @role = "button" })
                    </div>
                </div>
            </div>
        }
        </div>*@
